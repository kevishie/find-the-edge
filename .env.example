# Local application values (safe, non-secret defaults)
NODE_ENV=development
FTE_APPLICATION_NAME=find-the-edge
FTE_PORT=5173

# Future provider profile (secret; leave blank in fixture mode)
ODDS_API_KEY=

# Future AWS profile (deployment metadata; no account IDs or credentials)
AWS_REGION=
FTE_AWS_STAGE=
