# Design references

These HTML files are visual references only. Some are generated prototypes that
contain executable scripts and may fetch external assets when opened. Do not
serve them from the production application, import their runtimes into React, or
treat their embedded data and calculations as authoritative product logic.
