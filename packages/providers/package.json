{
  "name": "@find-the-edge/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src",
    "test": "vitest run src --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@find-the-edge/config": "workspace:*",
    "@find-the-edge/domain": "workspace:*",
    "@find-the-edge/observability": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/util-dynamodb": "3.996.7",
    "vitest": "^4.0.18"
  }
}
