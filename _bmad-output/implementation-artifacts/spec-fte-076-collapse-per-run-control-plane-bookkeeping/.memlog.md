---
topic: FTE-076 Collapse Per-Run Control-Plane Bookkeeping
updated: 2026-08-13T22:02
---

- (direction) Implement the FTE-076 constant-cost RUN bookkeeping contract while keeping deployment, completion, and savings claims gated on FTE-075.
- (constraint) FTE-075 must produce a valid recorded baseline before FTE-076 optimization is deployed, marked done, or used for any savings claim.
- (capability) CAP-1: RUN-partition traffic for a full pass becomes constant in page count; fresh success is exactly two accesses, resumed success at most three, and early losers or skips zero.
- (capability) CAP-2: PAGE evidence intent and commit markers remain the exactly-once authority while per-page evidence transitions stop mutating RUN.
- (capability) CAP-3: Schedule source and conflict evidence pages commit in one bounded page-only transaction when both are new, while partial replay commits only uncommitted pages.
- (capability) CAP-4: Contended workers establish continuation ownership before reading RUN or scanning PAGE ledger, so a losing worker performs zero RUN accesses.
- (capability) CAP-5: Never-read dependency, cadence, and quota skip RUN rows are replaced by bounded privacy-safe audit telemetry without losing incident-relevant status, reason, league, provider, policy version, or time.
- (constraint) Do not change paid request reservation, quota reconciliation, continuation leases, run lineage, provider attempts, immutable PAGE payloads, gap idempotency, or terminal run outcome fields.
- (decision) Use an explicit known-prior RUN compare-and-swap transition that returns the stored version; never replace version fencing with an unfenced update.
- (decision) No RUN existence condition remains inside per-page evidence transactions because RUN rows have no deletion path; a condition check would preserve page-proportional RUN reads.
- (decision) FTE-075 capacity middleware needs no change; it already reports operation and bounded prefix with INDEXES consumed capacity.
- (decision) Place the required spec folder under implementation-artifacts as explicitly directed, overriding the default specs output path.
- (constraint) High-risk idempotency work requires adversarial review of access counting, crash replay, and acceptance preservation before merge.
- (event) Coherence validation passed: five unique capabilities each have intent and testable success; constraints bend design; non-goals and concrete success signal are present; prose is lean.
- (event) Preservation validation passed: all load-bearing FTE-076 story, Epic 13, code-audit, access-budget, crash-replay, audit, dependency, risk, and file-scope claims land in the kernel or implementation companion; wrapper-only process prose was dropped.
- (event) Runtime implementation and counting regressions completed: a fresh 100-page evidence-bearing pass performs two RUN accesses, resumed performs three, losers perform zero, and 100 PAGE transitions perform no RUN access.
- (event) Two adversarial review iterations repaired empty-evidence recovery, legacy versionless CAS fencing, malformed lease reclaim, and acceptance-harness gaps; the full repository gate passed including 34 browser tests.
- (event) Sprint tracking and deployment remain untouched because the valid FTE-075 baseline gate is still closed.
