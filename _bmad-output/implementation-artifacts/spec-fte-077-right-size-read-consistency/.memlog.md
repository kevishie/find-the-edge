---
topic: FTE-077 Right-Size Read Consistency in the Control Plane
updated: 2026-08-13T22:02
---

- (direction) Implement the conservative FTE-077 consistency slice while keeping deployment, completion, and savings claims gated on FTE-075.
- (constraint) A valid recorded FTE-075 baseline is a hard prerequisite for optimization deployment, marking FTE-077 done, or publishing any capacity or savings claim.
- (capability) CAP-1: Every strong-consistency call site is explicitly classified, and each retained strong read names the fence, claim, version, evidence, checkpoint, authorization, or stale-index invariant it protects.
- (capability) CAP-2: Six observational list/history reads and two delay-only projection-readiness probes use eventual consistency; two durable-decision readiness probes and provider-health reads remain strong.
- (capability) CAP-3: Each downgraded path has a stale-read test proving it fails closed, returns timestamped observational data, or temporarily omits immutable history without mutating authority.
- (capability) CAP-4: FTE-075 measures before and after read units using identical valid windows, and rollback restores only the changed consistency flags if correctness or availability regresses.
- (decision) The audited baseline contains 76 explicit ConsistentRead true sites plus two default-strong gateway options.
- (decision) The conservative first wave downgrades watchlist list, odds-history query, strategy audit list, scouting report version list, cohort list, and lifecycle history; provider health remains strong because stale healthy state can mask a durable outage or exhausted capacity.
- (decision) API and live-odds readiness misses only delay work and are eventual; paper-pick and opportunity-generation readiness misses can cause durable state and therefore remain strong.
- (decision) Watchlist DELETE uses a bounded, requester-scoped, strongly consistent exact candidate lookup and never authorizes deletion from the eventual UI list.
- (constraint) Do not change reads participating in ownership, leases, optimistic versions, replay or idempotency, evidence decisions, checkpoints, authorization, entitlements, quota enforcement, or stale-GSI neutralization.
- (constraint) Gateway defaults remain strongly consistent; only audited observational callers opt out explicitly.
- (constraint) No data migration, backfill, table/index change, polling change, or provider cadence change is permitted.
- (decision) Retrospective list queries remain strong in the first wave because the shared helper currently treats stale indexes and cursor misses as corruption; any future downgrade requires separate behavior hardening.
- (decision) FTE-075 capacity attribution is reused unchanged; FTE-077 does not invent a baseline or forecast savings.
- (note) The full line-by-line consistency inventory, stale-read test matrix, observability, rollback, and file plan live in consistency-inventory.md.
- (event) Coherence validation passed: four unique capabilities have intent and testable success; constraints bend design; non-goals and a concrete measured success signal are present.
- (event) Preservation validation passed: all audited call sites, conservative downgrade decisions, protected invariants, stale-read tests, measurement gate, rollback, and no-claim boundary land in the kernel or companion; wrapper-only conversation prose was dropped.
- (event) Runtime implementation, stale/convergence regressions, and retained-strong invariant comments completed; sprint tracking and deployment remain untouched.
- (event) Blind and edge-case findings were repaired, an independent iteration-2 re-review returned CLEAN, and the full repository gate passed including 34 browser tests.
